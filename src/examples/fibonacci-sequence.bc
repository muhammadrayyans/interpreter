n = get, int("Enter a limit : ")
count = 0
first = 0
second = 1
next = 0

when(count <= n){
    assume(count <= 1){
        next = count
    }
    unless{
        next = int(first + second)
        first = second
        second = next
    }
    display(next)
    count = count + 1
}