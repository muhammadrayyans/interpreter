password = "secret"
user_enter = ""

when(1 == 1){
    user_enter = get("Enter password: ")
    assume(password == user_enter){
        display("User authenticated")
        stop
    }
    unless{
        display("Error user name or password is incorrect")
    }
}
